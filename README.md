Я не понял почему dockerfile не подгружается при commit
● Образ собирается введенем команды: "docker build -t {название образа} ."
● Контейнер можно запустить через docker descktop на вкладке "images", нажатием на кнопку run, либо введением команды "docker run --rm -p 8000:8000 {название образа} ". При запуске из десктопной версии, необходмо ввести "8000" в поле "Ports"
● какой URL проверить ([http://127.0.0.1:8000/docs))
